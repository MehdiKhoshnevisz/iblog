export enum STATUS_RESPONSE {
  SUCCESS = "succeeded",
  FAIL = "failed",
}

export enum REACTION {
  LIKE = "+1",
  TADA = "tada",
  HEART = "heart",
  OPEN_MOUTH = "open_mouth",
}

export enum BUTTON_COLOR {
  error = "error",
  default = "default",
  primary = "primary",
  success = "success",
}
